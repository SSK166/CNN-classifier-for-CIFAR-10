# CNN-classifier-for-CIFAR-10
Lightweight CNN for CIFAR-10 image classification with data augmentation, filter visualization, and performance comparison against classical ML models ( SVM, KNN, Random Forest).
